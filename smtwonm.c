#include <stdio.h>

int main(void) {
	int a,b,c;
	scanf("%d",&a);
              printf("the first value%d",a);
	scanf("%d",&b);
	printf("the second value%d",b);
	c=a+b;
	printf("print the total value of%d",c);
	
	return 0;
}
