#include<stdio.h>
int main()
{
	int sum=0,a=1,n,i,s=0;
	 printf("enter the series number");
	 scanf("%d",&n);
	 printf("%d%d",a,s);
	 for(i=o;i<n;i++)
	 {
	 	
	 	
	 	
	 	sum=s+a;
	 	s=a;
	 	a=sum;
	 	
	 	
	 }
	 printf("\n%d",sum);
	 return 0;
}
